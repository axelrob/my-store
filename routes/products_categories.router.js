/*const express=require('express');
const router=express.Router();

app.get('/:productsId/categories/:categoriesId', (req, res) =>{
  const {productsId,categoriesId} = req.params;
  res.json({
    productsId,
    categoriesId
  });
})

module.exports=router;
*/
